<?xml version="1.0" encoding="utf-8"?>
<!-- Swarm E: Deal-of-the-day AppWidget config. Update via WorkManager (DealWidgetWorker,
     6h) — updatePeriodMillis=0 to save battery. Preview + description for picker. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_deal_desc"
    android:initialLayout="@layout/widget_deal"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:previewImage="@mipmap/ic_launcher"
    android:previewLayout="@layout/widget_deal"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
